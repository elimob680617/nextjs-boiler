import React from "react";
import { TextboxProps } from "@/app/_components/textbox/textbox.types";

const Textbox: TextboxProps = ({}: {}) => {
  return <div>Textbox: TextboxProps</div>;
};
export default Textbox;
